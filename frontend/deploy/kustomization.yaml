apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: k8s-manager

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: k8s-manager-web
  app.kubernetes.io/component: web

apiVersion: v1
kind: Service
metadata:
  name: k8s-manager-web
  namespace: k8s-manager
  labels:
    app: k8s-manager-web
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30080
  selector:
    app: k8s-manager-web

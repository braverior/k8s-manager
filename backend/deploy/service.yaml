apiVersion: v1
kind: Service
metadata:
  name: k8s-manager
  namespace: k8s-manager
  labels:
    app: k8s-manager
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30001
  selector:
    app: k8s-manager

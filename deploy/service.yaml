apiVersion: v1
kind: Service
metadata:
  name: k8s-manager
  namespace: default
  labels:
    app: k8s-manager
spec:
  type: NodePort
  selector:
    app: k8s-manager
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30000
      protocol: TCP
